import libkurt

fn main() -> i32 {
    let mut i = 0
    while i < argc() {
        if i > 0 {
            print(" ")
        }
        print(arg(i))
        i = i + 1
    }
    println("")
    return 0
}
