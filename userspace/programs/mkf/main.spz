import libkurt

fn main() -> i32 {
    if argc() < 1 {
        println("usage: mkf <path> [text]")
        return 2
    }
    if argc() < 2 {
        return fs_write(arg(0), "")
    }
    return fs_write(arg(0), arg(1))
}
